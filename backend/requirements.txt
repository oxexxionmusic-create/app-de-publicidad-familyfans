# requirements.txt
# Backend - Family Fans Mony API

# Framework y servidor
fastapi==0.110.1
uvicorn[standard]==0.25.0

# Base de datos asíncrona MongoDB
motor==3.3.1
pymongo==4.5.0

# Autenticación y seguridad
PyJWT==2.12.1
bcrypt==4.1.3
python-multipart==0.0.24

# Manejo de archivos asíncrono
aiofiles==25.1.0

# Variables de entorno
python-dotenv==1.2.2

# Cloudinary SDK (gestión de medios)
cloudinary==1.41.0

# Procesamiento de video (para video_compression.py)
ffmpeg-python==0.2.0

# Utilidades para fechas y validaciones (usado en modelos Pydantic)
python-dateutil==2.9.0.post0
email-validator==2.3.0

# Cliente HTTP (por si se necesita en tareas de fondo, no estrictamente requerido)
requests==2.33.1

# (Opcional) Para tareas asíncronas programadas, aunque el backend usa asyncio.create_task
# apscheduler==3.10.4

# NOTA: Las siguientes dependencias son requeridas por algunas funcionalidades pero ya vienen incluidas
# con FastAPI o son parte de la biblioteca estándar. Se listan solo para referencia.
# - typing, datetime, os, logging, random, string, pathlib (Python estándar)
# - bson (parte de pymongo)
# - starlette (FastAPI depende de él)
